# http-level directives (this file is included inside http {} by the nginx image)
limit_req_zone $binary_remote_addr zone=general:1m rate=10r/s;
limit_req_zone $binary_remote_addr zone=uploads:1m rate=1r/m;
limit_req_zone $binary_remote_addr zone=demo:1m rate=2r/m;
server_tokens off;

upstream backend {
  server ${BACKEND_HOST}:${BACKEND_PORT};
  keepalive 16;
}

server {
  listen 8080;
  root /usr/share/nginx/html;
  client_max_body_size ${MAX_UPLOAD_SIZE}g;
  client_body_timeout 300s;
  limit_req_status 429;

  error_page 429 /429.json;
  location = /429.json {
    internal;
    default_type application/json;
    return 429 '{"detail": "Too many requests"}';
  }

  gzip on;
  gzip_vary on;
  gzip_proxied any;
  gzip_comp_level 6;
  gzip_min_length 256;
  gzip_types text/plain text/css text/javascript application/javascript
             application/json application/xml image/svg+xml;

  # Merge with location-level add_header instead of replacing (nginx 1.29.3+)
  add_header_inherit merge;
  include /etc/nginx/security-headers.conf;

  # Upload init - strictest limit (creates server-side state)
  location = /api/v1/users/upload/init {
    limit_req zone=uploads burst=2 nodelay;
    include /etc/nginx/proxy-params.conf;
  }

  # Upload complete - heavy processing, strict limit
  location ~ ^/api/v1/users/upload/[^/]+/complete$ {
    limit_req zone=uploads burst=2 nodelay;
    include /etc/nginx/proxy-params.conf;
  }

  # Upload chunks - gated by init, moderate rate limit
  location /api/v1/users/upload/ {
    limit_req zone=general burst=30 nodelay;
    include /etc/nginx/proxy-params.conf;
  }

  # Legacy single-upload endpoint
  location = /api/v1/users/upload {
    limit_req zone=uploads burst=2 nodelay;
    include /etc/nginx/proxy-params.conf;
  }

  # Demo endpoint - prevent mass account creation
  location = /api/v1/users/demo {
    limit_req zone=demo burst=2 nodelay;
    include /etc/nginx/proxy-params.conf;
  }

  # General API - moderate limit
  location /api/ {
    limit_req zone=general burst=30 nodelay;
    include /etc/nginx/proxy-params.conf;
  }

  # Vite-hashed assets - immutable, cache forever
  location /assets/ {
    add_header Cache-Control "public, max-age=31536000, immutable";
  }

  location / {
    index index.html index.htm;
    try_files $uri $uri/ /index.html;
    add_header Cache-Control "no-cache";
  }
}
